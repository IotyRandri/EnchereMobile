import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'enchere_mobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
